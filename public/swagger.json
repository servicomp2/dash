{
    "openapi": "3.0.0",
    "info": {
        "title": "Dash API",
        "description": "Documentación Automática de API REST generada por Dash",
        "version": "1.0.0"
    },
    "servers": [
        {
            "url": "/",
            "description": "Servidor Principal"
        }
    ],
    "components": {
        "securitySchemes": {
            "bearerAuth": {
                "type": "http",
                "description": "Ingresa el token provisto tras iniciar sesión en el formato: Bearer {token}",
                "bearerFormat": "JWT",
                "scheme": "bearer"
            }
        }
    }
}